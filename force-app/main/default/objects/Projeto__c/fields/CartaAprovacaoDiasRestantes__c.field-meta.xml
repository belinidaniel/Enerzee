<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>CartaAprovacaoDiasRestantes__c</fullName>
    <description>Dias até a validade da carta (valor negativo indica vencimento).</description>
    <externalId>false</externalId>
    <label>Dias Restantes para a Carta</label>
    <formula>IF(
    ISBLANK(CartaAprovacaoValidade__c),
    NULL,
    CartaAprovacaoValidade__c -
    IF(
        NOT(ISBLANK(Instalacao__r.DateInspection__c)),
        DATEVALUE(Instalacao__r.DateInspection__c),
        TODAY()
    )
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
