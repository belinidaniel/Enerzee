<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Oportunidade_Must_Match_Projeto</fullName>
    <active>false</active>
    <description>Garante consistência entre Oportunidade da instalação e Oportunidade do projeto, com bypass para perfil administrador (EN/PT-BR).</description>
    <errorConditionFormula>AND(
NOT(
OR(
LOWER($Profile.Name) = "system administrator",
LOWER($Profile.Name) = "administrador do sistema"
)
),
AND(
NOT(ISBLANK(Oportunidade__c)),
NOT(ISBLANK(Projeto__c)),
Oportunidade__c &lt;&gt; Projeto__r.Oportunidade__c
)
)</errorConditionFormula>
    <errorMessage>A Oportunidade da Instalação deve ser a mesma Oportunidade do Projeto.</errorMessage>
</ValidationRule>
